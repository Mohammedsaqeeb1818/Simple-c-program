# Simple-c-program
#include<stdio.h>
int main()
{
int a,b,sum;
printf("Enter the number A:");
scanf("%d",&a);
printf("Enter the number B");
scanf("%d",&b);
sum=a+b;
printf("The sum of two number's is:%d",sum);
}
